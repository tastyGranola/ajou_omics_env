# 세션 B — 단계별 쿼리 · human-in-the-loop

실행 위치: `worktrees/stepwise-hitl/`

```bash
cd worktrees/stepwise-hitl && claude
```

---

## 여는 프롬프트 — 그대로 붙여 넣으세요

세션 A 와 **목표 문장은 똑같습니다.** 달라지는 것은 진행 방식뿐입니다.

```
data/processed/gse96583_ifnb.h5ad 를 써서 이 질문에 답할 거야.

  IFN-beta 자극을 받은 PBMC 는 세포 타입별로 어떻게 다르게 반응하는가?

계획을 한 번에 세우지 말고 한 단계씩 가자. QC 부터 시작해.

각 단계가 끝나면 (1) 무엇을 했고 어떤 수치가 나왔는지, (2) 여기서 판단이 갈리는
지점과 선택지 2~3개를 근거와 함께 보여주고 멈춰. 내가 고르면 다음 단계로 가.
```

## 각 단계에서 하는 일

Claude 가 선택지를 내놓으면 **고르기 전에 근거를 한 번 되물어 보세요.**

```
2번을 고르면 뒤 단계에서 뭐가 달라져?
```

```
그 컷오프로 잘려나가는 세포가 어떤 세포들인지 먼저 보여줘.
```

고른 뒤에는 진행시킵니다.

```
2번으로 가자. 그리고 이 결정 EXPERIMENT.md 에 남겨줘 — 내가 골랐다고.
```

## 고르기 전에 검증자를 부를 수 있습니다

세션 A 와 **같은 검증자**(`step-validator`)가 이 작업 트리에도 걸려 있습니다.
Claude 가 선택지를 내놓았을 때, 고르기 전에 방금 끝난 단계를 검증시켜 보세요.

```
고르기 전에, 방금 끝낸 단계를 step-validator 로 채점해줘.
결과는 results/validation/<번호>_<단계>.md 에 저장하고,
정확성·완결성 점수와 고쳐야 할 것만 보여줘.
```

채점자는 **정확성**과 **완결성**을 각각 1–5 점으로 매깁니다. 정확성 2점 이하면
`FAIL`(진행 금지), 둘 다 4점 이상이면 `PASS`, 나머지는 `WARN` 입니다.

점수를 보고 고르면 선택의 근거가 달라집니다. **그 차이가 A 와 비교할 재료입니다.**
A 는 같은 점수를 혼자 읽고 혼자 반영했고, 여기서는 여러분이 읽습니다.

완결성 점수가 낮게 나왔다면 이렇게 되물어 보세요.

```
완결성 점수를 깎은 항목이 뭐야? 그거 지금 채우면 선택이 달라질까?
```

> 채점자도 Claude 입니다. 5점이 옳다는 뜻은 아닙니다.
> 높은 점수를 준 항목 중 미심쩍은 것이 있으면 그것을 되물어 보세요.

## 단계 목록 — 대략 이 순서로 갑니다

| | 단계 | 흔히 갈리는 지점 |
|---|---|---|
| 1 | QC | `n_genes` 상한을 둘 것인가 · 컷오프를 어디로 |
| 2 | 정규화 · HVG | HVG 개수 · flavor |
| 3 | 배치 통합 | `stim` 을 배치로 보정할 것인가 (조건 효과가 지워질 위험) |
| 4 | Clustering | resolution — 클러스터를 몇 개로 볼 것인가 |
| 5 | Annotation | marker 근거 · 애매한 클러스터를 합칠 것인가 나눌 것인가 |
| 6 | 차등발현 | 세포 타입 안에서 볼 것인가 · pseudobulk 로 갈 것인가 |

**3번에서 세션 A 와 크게 갈릴 가능성이 높습니다.** 그 장면을 기억해 두세요.

## 끝났을 때 확인

```
results/summary/metrics.json 과 EXPERIMENT.md 결정 로그를 채워줘.
내가 고른 것과 네가 고른 것을 decided_by 로 구분해줘.
채점을 돌린 단계는 metrics.json 의 validation 블록에 점수를 남겨줘.
```
